// simple test file

c :: class
	init :: fn(self, name)
		self.name := name
	/fn

	greet :: fn(self, x)
		print(x, ' HEY ', self.name)
	/fn
/class

i :: c('me!')
i.greet('first')

f := i.greet
f('second')
